void main() {
  /// Null Safety is the method that can you perform to prevent the variable value to be null
  // String? nama;
  // print(nama!);

  /// Late
  /// late used to create a empty variables but you need to specify the data in the future.
  // late String eat;
  // eat = "at 09.00";
  // print(eat);
}
